syntax = "proto3";

package calculator;

service Calculator {
  rpc Sumar (SumaRequest) returns (SumaResponse);
}

message SumaRequest {
  double a = 1;
  double b = 2;
}

message SumaResponse {
  double resultado = 1;
}