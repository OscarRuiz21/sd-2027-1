services:

  server:
    build: ./server
    container_name: t01_server
    ports:
      - "5000:5000"
      - "50051:50051"

  client:
    build: ./client
    container_name: t01_client
    environment:
      SERVER_HOST: server
    depends_on:
      - server