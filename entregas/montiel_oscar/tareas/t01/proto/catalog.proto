syntax = "proto3";

package catalog;

// Contrato del adaptador gRPC para consultar artículos del catálogo.
service Catalog {
  rpc GetItem (ItemRequest) returns (ItemReply);
}

message ItemRequest {
  string id = 1;
}

message ItemReply {
  string id = 1;
  string name = 2;
  double price = 3;
}
