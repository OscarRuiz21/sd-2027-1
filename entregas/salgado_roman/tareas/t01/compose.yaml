services:
  servidor:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "8000:8000"
      - "50051:50051"
      
  cliente:
    build:
      context: .
      dockerfile: Dockerfile.client
    depends_on:
      - servidor