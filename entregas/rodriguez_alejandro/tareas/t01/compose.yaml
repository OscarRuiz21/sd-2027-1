version: '3.8'
services:
  server:
    build: 
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "8080:8080" # REST
      - "9090:9090" # gRPC

  client:
    build: 
      context: .
      dockerfile: client/Dockerfile
    depends_on:
      - server