services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "5000:5000"
      - "50051:50051"

  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    environment:
      SERVIDOR_HOST: server
    depends_on:
      - server